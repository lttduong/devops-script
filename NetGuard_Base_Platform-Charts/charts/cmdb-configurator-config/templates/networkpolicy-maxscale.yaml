{{- if (.Values.geo_redundancy.enabled) }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{.Release.Name}}-maxscale
spec:
  ingress:
  - ports:
    - port: 8989
      protocol: TCP
  podSelector:
    matchLabels:
      type: maxscale
  policyTypes:
  - Ingress
{{- end }}