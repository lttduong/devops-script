apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-top-level-upgrade
  annotations:
    "helm.sh/hook": post-upgrade
    "helm.sh/hook-weight": "-1"
    "helm.sh/hook-delete-policy": before-hook-creation, hook-succeeded
stringData:
{{ (tpl (.Files.Glob "config_files/top-level/*").AsConfig . ) | nindent 2 }}
