apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.global.releaseNamePrefix }}{{ template "name" . }}-customcli-mail
  labels:
    app: {{ template "name" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
data:
{{ (tpl (.Files.Glob "customcli-mail-configmap/05-mail.cli").AsConfig . ) | indent 2 }}
