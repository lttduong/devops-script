apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.global.releaseNamePrefix }}{{ template "name" . }}-keycloak
  labels:
    app: {{ template "name" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
data:
{{ (tpl (.Files.Glob "keycloak-configmap/*").AsConfig . ) | indent 2 }}
