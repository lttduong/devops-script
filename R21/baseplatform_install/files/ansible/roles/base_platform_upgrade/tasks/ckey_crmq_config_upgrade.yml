---
- name: "Upgrading CKEY CRMQ configurator chart"
  shell: >
    {{ helm }}  upgrade {{ ckeyCrmqConfiguratorHelmName }} {{ chartsPath }}/ckey/crmq-config
    --namespace {{ BP_CONFIG_NAMESPACE }}
    --reuse-values
    --values {{ chartsPath }}/values/upgrade-global-values.yaml
    --install
    --wait
    --timeout {{ wait_timeout }}s
  no_log: "{{ no_log }}"
