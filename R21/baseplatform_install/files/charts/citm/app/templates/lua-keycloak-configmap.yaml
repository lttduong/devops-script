## Start of custom chart file
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "controller.fullname" . }}-lua-keycloak
  labels:
    backup: "true"
data:
{{ (tpl (.Files.Glob "custom_lua/keycloak.lua").AsConfig . ) | indent 2 }}
## End of custom chart file
