apiVersion: v1
kind: Secret
metadata:
  name: {{ .Chart.Name }}
  labels:
    backup: "true"
type: Opaque
data:
  mariadb-password: {{ .Values.mariadb.password | b64enc | quote }}
  mariadb-user: {{ .Values.mariadb.username | b64enc | quote }}
