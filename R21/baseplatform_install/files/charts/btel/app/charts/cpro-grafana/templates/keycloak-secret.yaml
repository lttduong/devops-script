{{- if not .Values.keycloak.secret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "grafana.fullname" . }}-keycloakcrt
type: Opaque
data:
  keycloak.crt: {{ .Values.keycloak.cert }}
{{- end }}
