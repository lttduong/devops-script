{{- if and .Values.istio.enable .Values.pushgateway.enabled}}
{{- if .Values.pushgateway.istioIngress.enabled }}
{{- if eq .Values.pushgateway.istioIngress.gatewayName "" }}
apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  labels:
    app: {{ template "prometheus.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    component: "{{ .Values.pushgateway.name }}"
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
{{- include "prometheus-labels" (tuple .Values.global.labels) | indent 4}}
  name: {{ template "prometheus.pushgateway.fullname" . }}-istio-gateway
{{- if .Values.global.annotations }}
  annotations:
{{- include "prometheus-annotations" (tuple .Values.global.annotations) | indent 4}}
{{- end }}
spec:
  {{- if .Values.pushgateway.istioIngress.selector }}
  selector:
{{ toYaml .Values.pushgateway.istioIngress.selector | indent 4 }}
  {{- end }}
  servers:
  {{- if .Values.pushgateway.istioIngress.tls.enabled }}
  - hosts:
    - '{{ .Values.pushgateway.istioIngress.host }}'
    port:
      name: http
      number: {{ .Values.pushgateway.istioIngress.httpPort }}
      protocol: HTTP
    tls:
      httpsRedirect: true # sends 301 redirect for http requests
  - hosts:
    - '{{ .Values.pushgateway.istioIngress.host }}'
    port:
      name: https
      number: {{ .Values.pushgateway.istioIngress.tls.httpsPort }}
      protocol: HTTPS
    tls:
      {{- if eq .Values.pushgateway.istioIngress.tls.mode "PASSTHROUGH" }}
      mode: PASSTHROUGH
      {{- else if eq .Values.pushgateway.istioIngress.tls.mode "SIMPLE" }}
      credentialName : {{ .Values.pushgateway.istioIngress.tls.credentialName}}
      mode: SIMPLE
      {{- else if eq .Values.pushgateway.istioIngress.tls.mode "MUTUAL" }}
      credentialName : {{ .Values.pushgateway.istioIngress.tls.credentialName}}
      mode: MUTUAL
      {{- else if eq .Values.pushgateway.istioIngress.tls.mode "ISTIO_MUTUAL" }}
      {{- if eq .Values.pushgateway.istioIngress.tls.credentialName "" }}
      mode: ISTIO_MUTUAL
      {{- else}}
      mode: ISTIO_MUTUAL
      credentialName : {{ .Values.pushgateway.istioIngress.tls.credentialName}}
      {{- end }}
      {{- end }}
  {{- else }}
  - hosts:
    - '{{ .Values.pushgateway.istioIngress.host }}'
    port:
      name: http
      number: {{ .Values.pushgateway.istioIngress.httpPort }}
      protocol: HTTP
  {{- end }}
{{- end }}
{{- end }}
{{- end }}
