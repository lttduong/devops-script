{{- if .Values.istio.enable }}
{{- if .Values.server.istioIngress.enabled }}
{{- if eq .Values.server.istioIngress.gatewayName "" }}
apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  labels:
    app: {{ template "prometheus.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    component: "{{ .Values.server.name }}"
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
{{- include "prometheus-labels" (tuple .Values.global.labels) | indent 4}}
  name: {{ template "prometheus.server.fullname" . }}-istio-gateway
{{- if .Values.global.annotations }}
  annotations:
{{- include "prometheus-annotations" (tuple .Values.global.annotations) | indent 4}}
{{- end }}
spec:
  {{- if .Values.server.istioIngress.selector }}
  selector:
{{ toYaml .Values.server.istioIngress.selector | indent 4 }}
  {{- end }}
  servers:
  {{- if .Values.server.istioIngress.tls.enabled }}
  - hosts:
    - '{{ .Values.server.istioIngress.host }}'
    port:
      name: http
      number: {{ .Values.server.istioIngress.httpPort }}
      protocol: HTTP
    tls:
      httpsRedirect: true # sends 301 redirect for http requests
  - hosts:
    - '{{ .Values.server.istioIngress.host }}'
    port:
      name: https_prom
      number: {{ .Values.server.istioIngress.tls.httpsPort }}
      protocol: HTTPS
    tls:
      {{- if eq .Values.server.istioIngress.tls.mode "PASSTHROUGH" }}
      mode: PASSTHROUGH
      {{- else if eq .Values.server.istioIngress.tls.mode "SIMPLE" }}
      credentialName : {{ .Values.server.istioIngress.tls.credentialName}}
      mode: SIMPLE
      {{- else if eq .Values.server.istioIngress.tls.mode "MUTUAL" }}
      credentialName : {{ .Values.server.istioIngress.tls.credentialName}}
      mode: MUTUAL
      {{- else if eq .Values.server.istioIngress.tls.mode "ISTIO_MUTUAL" }}
      {{- if eq .Values.server.istioIngress.tls.credentialName "" }}
      mode: ISTIO_MUTUAL
      {{- else}}
      mode: ISTIO_MUTUAL
      credentialName : {{ .Values.server.istioIngress.tls.credentialName}}
      {{- end }}
      {{- end }}
  {{- else }}
  - hosts:
    - '{{ .Values.server.istioIngress.host }}'
    port:
      name: http
      number: {{ .Values.server.istioIngress.httpPort }}
      protocol: HTTP
  {{- end }}
{{- end }}
{{- end }}
{{- end }}
