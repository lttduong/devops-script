apiVersion: v1
kind: Secret
metadata:
  name: {{ .Chart.Name }}-config-file
  labels:
    backup: "true"
type: Opaque
stringData:
{{ ( tpl ( .Files.Glob "config_files/sql/*" ).AsConfig . ) | nindent 2 }}
