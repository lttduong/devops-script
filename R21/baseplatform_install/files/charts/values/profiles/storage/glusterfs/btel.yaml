belk:
  persistence:
    storageClassName: glusterfs-storageclass
  belk-elasticsearch:
    elasticsearch_master:
      persistence:
        createPv: false
    esdata:
      persistence:
        createPv: false

calm:
  persistence:
    createPv: false
    storageClass: glusterfs-storageclass

cpro:
  alertmanager:
    persistence:
      createPv: false
      storageClass: glusterfs-storageclass
  server:
    persistence:
      createPv: false
      storageClass: glusterfs-storageclass

grafana:
  persistence:
    createPv: false
    storageClass: glusterfs-storageclass

gen3gppxml:
  persistence:
    createPv: false
    storageClass: glusterfs-storageclass
