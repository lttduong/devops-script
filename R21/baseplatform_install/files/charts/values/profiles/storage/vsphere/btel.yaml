belk:
  persistence:
    storageClassName: vsphere
  belk-elasticsearch:
    elasticsearch_master:
      persistence:
        createPv: false
    esdata:
      persistence:
        createPv: false

calm:
  persistence:
    createPv: false
    storageClass: vsphere

cpro:
  alertmanager:
    persistentVolume:
      storageClass: vsphere
    persistence:
      createPv: false
  server:
    persistentVolume:
      storageClass: vsphere
    persistence:
      createPv: false

grafana:
  persistence:
    createPv: false
    storageClass: vsphere

gen3gppxml:
  persistence:
    createPv: false
    storageClass: vsphere
