belk:
  persistence:
    storageClassName: cinder-az-nova
  belk-elasticsearch:
    elasticsearch_master:
      persistence:
        createPv: false
    esdata:
      persistence:
        createPv: false

calm:
  persistence:
    createPv: false
    storageClass: cinder-az-nova

cpro:
  alertmanager:
    persistentVolume:
      storageClass: cinder-az-nova
    persistence:
      createPv: false
  server:
    persistentVolume:
      storageClass: cinder-az-nova
    persistence:
      createPv: false

grafana:
  persistence:
    createPv: false
    storageClass: cinder-az-nova

gen3gppxml:
  persistence:
    createPv: false
    storageClass: cinder-az-nova
